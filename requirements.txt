# Databricks and Spark-related dependencies
pyspark==3.1.2  # Ensure this version matches your Databricks cluster version
databricks-api==0.11.1  # For interacting with Databricks REST API

# AWS SDK for Python
boto3==1.20.0  # AWS SDK to interact with AWS services (e.g., S3, RDS)

# PostgreSQL database connector
psycopg2==2.9.3  # PostgreSQL adapter for Python

# Pandas for data manipulation
pandas==1.3.3  # Pandas for data handling (used in ETL transformations)

# Python environment management (optional but recommended)
python-dotenv==0.19.2  # For loading environment variables from .env files

# Additional packages for data validation and testing
pytest==6.2.5  # Testing framework (optional, if you need to add tests)
